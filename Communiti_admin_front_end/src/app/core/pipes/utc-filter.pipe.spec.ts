import { UtcFilterPipe } from './utc-filter.pipe';

describe('UtcFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UtcFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
